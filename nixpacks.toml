[phases.build]
cmds = ["pip install -r llm_assistant/backend/requirements.txt"]

[phases.setup]
nixPkgs = ["python311"]

[start]
cmd = "cd llm_assistant/backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
